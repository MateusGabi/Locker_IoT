{
  "name": "locker_iot",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "start:watch": "nodemon *.js --exec 'npm start'",
    "prettify": "prettier --single-quote --trailing-comma es5 --write '{app,__{tests,mocks}__}/**/*.js'"
  },
  "author": "Vitoria",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^1.18.4"
  },
  "devDependencies": {
    "prettier": "1.18.2"
  }
}
